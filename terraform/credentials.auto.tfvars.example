# Copy to credentials.auto.tfvars (gitignored) and fill in the token secret.
# Reuse the same Proxmox token as terraform-lab if it can create VMs on data01.
proxmox_api_token = "terraform@pve!provider=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
