---
# After restructure: apps + edge + mail + object storage + Dockhand/Portainer on docker-01.
- name: Docker app host (proxy, mail, tools, Dockhand, Portainer, AIStor, OpenClaw)
  hosts: docker-01
  become: true
  roles:
    - guest_common
    - docker_host
    - proxy
    - stalwart
    - it_tools
    - mailpit
    - aistor
    - dockhand
    - portainer
    - openclaw