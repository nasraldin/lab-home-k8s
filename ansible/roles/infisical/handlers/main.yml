---
- name: Restart Infisical stack
  ansible.builtin.command:
    cmd: docker compose -f {{ infisical_compose_dir }}/docker-compose.yml up -d
  args:
    chdir: "{{ infisical_compose_dir }}"
  become: true
