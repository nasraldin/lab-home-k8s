# AIStor Free license (gitignored)

Place your MinIO AIStor Free license here as:

```text
ansible/files/aistor/minio.license
```

Sources:

1. Download from https://min.io/download (AIStor Free)
2. Or copy from another lab checkout if you already have one:
   `cp ../ansible-lab/files/aistor/minio.license ansible/files/aistor/`

Never commit `minio.license`. The directory is gitignored in `.gitignore`.
